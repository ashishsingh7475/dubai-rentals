Build advanced search, filtering, and sorting for property listings.

Requirements:

* Filter by price range (min and max)
* Filter by Dubai area
* Filter by property type (room, apartment)
* Filter by bedrooms
* Filter by furnished
* Sorting options:

  * Newest
  * Price low to high
  * Price high to low
* Query parameters in URL for SEO
* Server-side filtering

Performance:

* Efficient Supabase queries
* Pagination or infinite scroll
* Avoid overfetching
* Fast response

UI:

* Modern Tailwind filter sidebar (desktop)
* Mobile filter drawer
* Sticky filter panel
* Reset filters
* Loading states
* Empty result state
* Clean modern design

Architecture:

* Server components
* URL-based search state
* Reusable filter components
* Strong typing

Output:

* Search page
* Filter components
* Sorting system
* URL sync
* Scalable structure
