Build authentication for this project using Supabase.

Requirements:

* Email and password signup and login
* Google OAuth login
* Form validation
* Error handling
* Loading states
* Mobile-first UI
* Modern design
* Tailwind CSS
* Protected routes
* Session persistence
* Logout functionality

Architecture:

* Use Supabase client
* Create reusable auth components
* Store user session globally
* Redirect authenticated users to dashboard
* Redirect unauthenticated users to login